# parse_ida_types.py
# parse ida types into generic types more aligned with those consumable by llvm

# takes a set of stack_locals as per collect_ida, returns a set of stack_locals
# with the ida types converted to more generic types (TODO: spec)
# TODO: impl
def parse_type(stack_locals):
  return stack_locals


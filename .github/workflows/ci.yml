name: CI
on:
  push:
    branches:
      - master
  pull_request:
  schedule:
    # run CI every day even if no PRs/merges occur
    - cron:  '0 6 * * *'
jobs:
  MacOS:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v1
      name: Cache LLVM 4 artifacts
      with:
        path: ccache_llvm40
        key: ${{ runner.os }}-llvm4
        restore-keys: |
          ${{ runner.os }}-llvm4
    - uses: actions/cache@v1
      name: Cache LLVM 5 artifacts
      with:
        path: ccache_llvm50
        key: ${{ runner.os }}-llvm5
        restore-keys: |
          ${{ runner.os }}-llvm5
    - uses: actions/cache@v1
      name: Cache LLVM 6 artifacts
      with:
        path: ccache_llvm60
        key: ${{ runner.os }}-llvm6
        restore-keys: |
          ${{ runner.os }}-llvm6
    - name: Run Tests
      run: |
        ./scripts/travis.sh macos-latest initialize
        ./scripts/travis.sh macos-latest build
  Docker_Linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        llvm: ["800", "900"]
        ubuntu: ["20.04", "19.10", "18.04"]
    steps:
    - uses: actions/checkout@v2
    - name: Build LLVM ${{ matrix.llvm }} on ${{ matrix.ubuntu }}
      run: |
        docker build . -t docker.pkg.github.com/lifting-bits/mcsema/llvm${{ matrix.llvm }}-ubuntu${{ matrix.ubuntu }}-amd64:latest -f Dockerfile --build-arg DISTRO_BASE=ubuntu${{ matrix.ubuntu }} --build-arg ARCH=amd64 --build-arg LLVM_VERSION=${{ matrix.llvm }}
    - name: Push Image for LLVM ${{ matrix.llvm }} on ${{ matrix.ubuntu }}
      if: github.event_name == 'push' && github.ref == 'refs/heads/master'
      run: |
        docker login docker.pkg.github.com -u "${GH_USER}" -p "${GH_TOKEN}"
        docker push docker.pkg.github.com/lifting-bits/mcsema/llvm${{ matrix.llvm }}-ubuntu${{ matrix.ubuntu }}-amd64:latest
        docker login -u "${DOCKER_HUB_USER}" -p "${DOCKER_HUB_TOKEN}"
        docker tag docker.pkg.github.com/lifting-bits/mcsema/llvm${{ matrix.llvm }}-ubuntu${{ matrix.ubuntu }}-amd64:latest trailofbits/mcsema:llvm${{ matrix.llvm }}-ubuntu${{ matrix.ubuntu }}-amd64
        docker push trailofbits/mcsema:llvm${{ matrix.llvm }}-ubuntu${{ matrix.ubuntu }}-amd64
      env:
        GH_USER: ${{ github.actor }}
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DOCKER_HUB_USER: ${{  secrets.DOCKER_HUB_USER  }}
        DOCKER_HUB_TOKEN: ${{  secrets.DOCKER_HUB_TOKEN  }}
